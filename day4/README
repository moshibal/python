Project name and description: Differences in javascript(array) and python(lists)

Installation instructions:
#python
1.To run pyhton, you need python install in your machine
2.to run the code python3 filename
#javascript
1.To run javascript, you need node install in your machine
2.then npm install to install the dependencies, as i have used readlineSync module to read the user input from the console.
3.to run the code `node filename`

#Differences

1. In python list, we can access item with negative indexes, whereas in javascript we cannnot.
2. differences in methods

#i have created few games to play with, if you have above mentioned software installed , fell free to play.
